# URL base consumida pelo frontend (Vite)
# Em desenvolvimento, mantenha /api para usar o proxy local do Vite
VITE_API_BASE_URL=/api

# Alvo do proxy do Vite no modo dev
VITE_API_PROXY_TARGET=http://localhost:3001
