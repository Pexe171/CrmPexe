# URL base consumida pelo frontend (Vite).
# Em desenvolvimento, use /api para o proxy local.
VITE_API_BASE_URL=/api

# Alvo do proxy do Vite no modo dev (use 127.0.0.1 para evitar IPv6/localhost).
VITE_API_PROXY_TARGET=http://127.0.0.1:3001
